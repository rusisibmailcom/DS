
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	УстановкаВидимостиФормы();
КонецПроцедуры

&НаКлиенте
Процедура УстановкаВидимостиФормы()
	Если  Объект.ВидДС = ПредопределенноеЗначение("Перечисление.Вид_ДС.Кредитные")  Тогда
		Элементы.Лимит.Видимость = Истина;
	Иначе
		Элементы.Лимит.Видимость = Ложь;
	КонецЕсли;
КонецПроцедуры