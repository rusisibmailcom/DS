
&НаКлиенте
Процедура ДомПриИзменении(Элемент)
	ПодстановкаУлицаДом();
КонецПроцедуры

&НаКлиенте
Процедура УлицаПриИзменении(Элемент)
	ПодстановкаУлицаДом();
КонецПроцедуры

Процедура ПодстановкаУлицаДом() 
	Если ЗначениеЗаполнено(Объект.Дом) Тогда
		Объект.УлицаДом = "" + Объект.Улица + ", " + Объект.Дом;
	Иначе
		Объект.УлицаДом = "" + Объект.Улица;
	КонецЕсли;  
КонецПроцедуры